name: core-services
namespace: fleet-default

# Core services bundle - services that run on core nodes
# Includes monitoring, logging, and other always-on services

targetCustomizations:
- name: core-only
  clusterSelector:
    matchLabels:
      cluster: homelab
